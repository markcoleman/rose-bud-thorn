name: Apple CI

on:
  pull_request:
    paths:
      - "Package.swift"
      - "Sources/**"
      - "Tests/**"
      - "Fixtures/**"
      - "App/**"
      - "UITests/**"
      - "ci_scripts/**"
      - "RoseBudThorn.xcodeproj/**"
      - "RoseBudThorn.xcworkspace/**"
      - ".github/workflows/apple-ci.yml"
  push:
    branches:
      - main
    paths:
      - "Package.swift"
      - "Sources/**"
      - "Tests/**"
      - "Fixtures/**"
      - "App/**"
      - "UITests/**"
      - "ci_scripts/**"
      - "RoseBudThorn.xcodeproj/**"
      - "RoseBudThorn.xcworkspace/**"
      - ".github/workflows/apple-ci.yml"
  workflow_dispatch:

concurrency:
  group: apple-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Detect impacted CI areas
    runs-on: ubuntu-latest
    outputs:
      package: ${{ steps.filter.outputs.package }}
      app: ${{ steps.filter.outputs.app }}
      ui: ${{ steps.filter.outputs.ui }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Filter changed files
        id: filter
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        with:
          filters: |
            package:
              - 'Package.swift'
              - 'Sources/**'
              - 'Tests/**'
              - 'Fixtures/**'
            app:
              - 'App/**'
              - 'Sources/**'
              - 'RoseBudThorn.xcodeproj/**'
              - 'RoseBudThorn.xcworkspace/**'
              - 'ci_scripts/**'
            ui:
              - 'App/**'
              - 'Sources/**'
              - 'UITests/**'
              - 'RoseBudThorn.xcodeproj/**'
              - 'RoseBudThorn.xcworkspace/**'

  package-test:
    name: Swift package build + test
    runs-on: macos-26
    needs: changes
    if: needs.changes.outputs.package == 'true'
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Select latest stable Xcode
        uses: maxim-lobanov/setup-xcode@60606e260d2fc5762a71e64e74b2174e8ea3c8bd # v1
        with:
          xcode-version: latest-stable

      - name: Print toolchain versions
        run: |
          xcodebuild -version
          swift --version

      - name: Cache SwiftPM dependencies
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5
        with:
          path: |
            ~/.swiftpm
            ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-${{ runner.arch }}-spm-${{ hashFiles('Package.swift') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Resolve package dependencies
        run: swift package resolve

      - name: Build package (strict)
        run: swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors

      - name: Test package (strict)
        run: swift test --parallel -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors

  build-app:
    name: Build app (${{ matrix.lane }})
    runs-on: macos-26
    needs: changes
    if: needs.changes.outputs.app == 'true'
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - lane: iOS
            destination: generic/platform=iOS Simulator
          - lane: Mac Catalyst
            destination: platform=macOS,variant=Mac Catalyst

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Select latest stable Xcode
        uses: maxim-lobanov/setup-xcode@60606e260d2fc5762a71e64e74b2174e8ea3c8bd # v1
        with:
          xcode-version: latest-stable

      - name: Build app
        run: |
          xcodebuild \
            -project "RoseBudThorn.xcodeproj" \
            -scheme "RoseBudThorn Universal" \
            -destination "${{ matrix.destination }}" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            build

  ui-smoke:
    name: UI smoke test
    runs-on: macos-26
    needs: changes
    if: github.event_name == 'pull_request' && needs.changes.outputs.ui == 'true'
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Select latest stable Xcode
        uses: maxim-lobanov/setup-xcode@60606e260d2fc5762a71e64e74b2174e8ea3c8bd # v1
        with:
          xcode-version: latest-stable

      - name: Run UI smoke test
        run: |
          xcodebuild \
            -project "RoseBudThorn.xcodeproj" \
            -scheme "RoseBudThorn Universal" \
            -destination "platform=iOS Simulator,name=iPhone 17" \
            -resultBundlePath "/tmp/RoseBudThorn-ui-smoke.xcresult" \
            -only-testing:RoseBudThornUITests/RoseBudThornUITests/testFirstLaunchShowsOnboardingThenSkipOpensToday \
            test

      - name: Upload UI smoke xcresult on failure
        if: failure()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: xcresult-ui-smoke
          path: /tmp/RoseBudThorn-ui-smoke.xcresult
